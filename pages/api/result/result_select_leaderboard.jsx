import { executeQuery } from "@/app/DB/db";

export default async function handler(req, res) {
  var leaderboardSelect = "";

  try {
    if (req.method === "POST") {
    }
  } catch (error) {
    console.log(error);
  }
}
